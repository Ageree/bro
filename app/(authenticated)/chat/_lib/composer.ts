/**
 * Paper has no pills. The composer the chat writes on is the same hairline
 * rectangle as a field in the vault — square corners, ink border — and its
 * submit is the one inverted rectangle the brand allows.
 */
export const paperComposerClassName =
  "[&_[data-slot=input-group]]:rounded-none [&_[data-slot=input-group]]:border-foreground";

export const paperComposerSubmitClassName = "rounded-none";
