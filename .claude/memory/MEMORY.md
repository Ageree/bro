# Накопленная память

То, что стоило нам времени и чего не видно из кода: отменённые попытки,
симптомы с неочевидной причиной, места, где текст расходится с реальностью.
Правила проекта живут в `AGENTS.md` — сюда их не дублируем.

Одна строка на вывод, свежие сверху. Детали — в файле темы рядом, здесь
ссылка. Узнал такое — допиши строку и закоммить вместе с работой; индекс
держим короче 200 строк, устаревшее удаляем, а не копим.

## Темы

- `turns-not-starting.md` — вебхук отвечает 200, а ход агента не стартует: две причины, обе стоили деплоя.

## Выводы

- 2026-09-20 — Прогон под Node 22 валит `pnpm check` стеной `SyntaxError: Unexpected identifier` в файлах, которых правка не касалась, — на чистой ветке тоже. Это не регрессия: репозиторий требует Node 24 (`.node-version`). Увидел такое — проверь версию, прежде чем искать причину в диффе.
- 2026-09-20 — После перехода на eve 0.62 модель в Telegram регулярно отвечает обычным текстом вместо вызова `send_message`, а каналы такой текст глушили (`message.completed` переопределён) — человек не получал ничего. Теперь канал сам досылает недоставленный текст; каждое срабатывание пишет строку `console.warn`, по ней видно, как часто модель пропускает инструмент.
- 2026-09-19 — Строка `AGENTS.md` «This build has no browser capability» пришла из upstream OpenInstinct и для этого форка неверна: `browser_task` работает на Browser Use Cloud, с профилями, live view и секретами из сейфа. Правится в этом же PR; при подтягивании upstream она вернётся — проверять.
- 2026-09-19 — eve 0.62 сломала обратную совместимость в двух местах: плоский `agent/instrumentation.ts` больше не обнаруживается (конфигурация разложена по `agent/instrumentation/`), а в evals `t.judge.autoevals.closedQA(...)` → `t.judge(...)` и `t.newSession()` → `await t.session()`. Судья требует evaluation-модель.
- 2026-09-19 — Лимиты на время закрытой беты сняты через `USAGE_LIMITS` («off» по умолчанию): `messageQuotaGate` и `browserRunQuotaGate` возвращают `allowed: true`, не обращаясь к `countUsage`, поэтому `usage_counters` сейчас не накапливается вовсе. Значения `FREE_*`/`PAID_*` при этом на месте — они просто не применяются; `USAGE_LIMITS=on` возвращает прежнее поведение и текст пейволла (#133).
