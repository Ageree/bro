# Память проекта

Индекс накопленных выводов. Одна строка на факт, свежие сверху. Детали — в
файлах тем рядом, здесь только ссылка. Правила ведения — в `CLAUDE.md`,
раздел «Память между сессиями».

## Темы

- `harness-goals.md` — как устроены большие цели в `.harness/goals/` и как продолжить незаконченную.

## Выводы

- 2026-09-17 — `tenantId()` для хода без принципала молча возвращал `local-dev`, и все такие ходы сходились на одном пользователе: чужой Gmail, чужая память, чужие дела. Тем же ключом адресуются jobs, wakeups и orders. Предикат «общий идентификатор» теперь один, `tenantId()` бросает вместо того, чтобы угадывать. Умолчание вместо ошибки в коде, который разделяет людей, — это утечка, а не удобство (#115).
- 2026-09-17 — Второй экземпляр того же дефекта прятался за неявным `NODE_ENV`, который репозиторий нигде не выставляет. Ветка «если не прод, то…» без явной установки переменной — всегда включённая ветка (#115).
- 2026-09-17 — Долгая память схлопнута в один Supermemory, `convex/memories.ts` удалён (раньше в каждый ход вслепую вливалось ~22 КБ фактов). Вступление `README.md` всё ещё описывает Convex как хранилище долгой памяти (#115).
- 2026-09-20 — У облачных сессий Claude Code нет собственной памяти между запусками: auto memory лежит в `~/.claude/projects/<project>/memory/` и, по документации, «not shared across machines or cloud environments». Отсюда весь этот каталог.
- 2026-09-20 — Из настроек облачная сессия читает только закоммиченный `.claude/settings.json`; `~/.claude/settings.json` и `~/.claude/CLAUDE.md` туда не доезжают. Всё, что должно работать в облаке, лежит в репозитории.
- 2026-09-17 — Секреты из хранилищ приходят с переносом строки **посреди** значения, и `fetch` отвергает такой заголовок целиком. `.trim()` не помогает — вырезать нужно все пробельные символы (`agent/lib/browseruse.ts`, `convex/lib/browseruse.ts`, тот же приём у `OPENROUTER_API_KEY`). Новый ключ в новом клиенте — нормализовать так же (#114).
- 2026-09-17 — `BRO_PUBLIC_URL` в проде указывает на `brobro.tech`, а это **лендинг, другой проект Vercel**. Маршрут `/l` живёт в проекте агента, поэтому ссылка на подключение была валидной и отвечала 404. Origin берётся из собственного адреса агента, `BRO_LINK_ORIGIN` — переопределение (#112).
- 2026-09-17 — Проверки из `npm run check` выполняются в процессе и в принципе не видят «маршрут есть в коде, но не на том хосте». Такие вещи ловит только реальный запрос после выкатки — как `scripts/deploy.sh`, который дёргает `/l` и валит деплой на не-200.
- 2026-09-17 — У исходящего две воронки: `deliverHuman` (всё, что говорит агент) и `sendPhotonText` (приветственное письмо, инвайт в Telegram, пейволл, отказ от SMS — у них на руках нет тенанта). Запись только в первую оставила онбординг невидимым для e2e; писать в `testTranscript` обязаны обе.
