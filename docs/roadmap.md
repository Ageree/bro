# План работ над Бро

Состояние на 24.09.2026, после прогонов обоих бенчмарков (`docs/benchmarks/`).
Цель — топ-10 Assistant Benchmark: 7.3–7.5 в среднем по 14 оцениваемым
измерениям из 16 (личность не оценивается баллом, звонков у Бро нет — N/A),
сейчас 5.1. Задачи идут по убыванию выигрыша. Одна задача — один PR в `bro-next`,
потом перепрогон затронутых измерений. Сделанную задачу вычёркивайте здесь же в
том же PR.

Приоритеты: P0 ломает поручения или безопасность, P1 даёт больше всего баллов,
P2 — качество и долги.

## P0

### ~~1. Browser Use: очередь вместо шквала повторов, кредиты под контролем~~ (сделано)

Сделано: на 429 поручение встаёт в очередь (`queued`, поллер стартует его,
когда освободится браузер), не больше трёх стартов за ход, на 402 — честный
ответ и алерт владельцу. Тесты — `tests/agent/tools/browser-task.test.ts` и
`tests/agent/schedules/browser-poller.test.ts`. Осталось: параллельный прогон
10 браузерных кейсов вживую.

- Доказательства: 278 ответов 429 «Too many concurrent active sessions» за час,
  один ход повторял старт 77 раз. С 09:18 UTC 24.09 — 402 «Insufficient
  credits», все поручения на сайтах стояли до пополнения.
- Где: `agent/lib/browser-use/client.ts`, `agent/tools/browser_task.ts`,
  `agent/schedules/browser-runs.ts`, `db/services/browser-runs.ts`.
- Сделать: на 429 не повторять в ходе, а парковать поручение со статусом
  `queued` и `retryAt` (как повтор при капче); лимит стартов на ход; на 402 —
  типизированная ошибка, честное сообщение человеку и алерт владельцу.
- Готово: тесты на 429 и 402; параллельный прогон 10 браузерных кейсов без
  циклов повторов.

### ~~2. Необратимое — только через карточку одобрения~~ (сделано)

- Доказательства: `allowSubmit: true` в 21 сессии русского прогона — заявление
  на Госуслугах с авто-повтором каждые 20 минут, чек в «Мой налог», запись к
  врачу, заказ такси, отклики на hh.ru, всё без вопроса.
- Где: описание `browser_task` в `agent/tools/browser_task.ts`,
  `agent/instructions/content/autonomy.md`, `agent/lib/schedules/tools.ts`.
- Сделать: финальная кнопка записи, заявления, отклика, чека или заказа — только
  после нативной карточки одобрения, как `allowPayment`. Расписания не ставят
  `allowSubmit`. Лимит трат из #171 не отменяет вопрос для необратимого.
- Готово: эвалы «запиши к терапевту», «подай на справку», «откликнись на три
  вакансии» ведут к карточке с деталями.
- ~~Без вечных переспросов~~ (сделано): одна карточка на поручение и на его
  оплату (`chargeRub`, запас 10%, 100–1 000 ₽), постоянные разрешения
  `standing_permission` без карточки в разговоре, в фоне по-прежнему отказ;
  детали карточки в iMessage; одобрение оплаты на поручении в очереди
  сохраняется. Эвалы — `evals/agent/commitment.eval.ts`.
- ~~Дыры в одном подтверждении~~ (сделано): разрешение и наследование только
  в ходе человека (не на отчёт браузера), `continue` не из хода человека не
  правит подтверждённое поручение; после `Needs: none` подтверждение
  исчерпано; разрешение держит запуск на хосте и виде, без публичных
  суффиксов, до 30 000 ₽ за раз и с месячным пределом; оплаты по карточке и
  разрешению пишутся в `spend_entries` (`source`); понятные карточки
  `standing_permission` и `spend_limit` в Telegram, iMessage и веб-чате.

### ~~3. Итоги браузера доходят сами и с деталями~~ (сделано)

Сделано: поллер проверяет все открытые запуски по кругу (раньше 25 зависших
держали места до 45 минут), запрос кода запуск отдаёт сразу и Бро просит код
на ближайшем тике, итог несёт список позиций (`ITEMS`), недоставленный дольше
2 минут итог — лог и алерт владельцу. Осталось проверить вживую на Госуслугах
и корзине Ozon.

- Доказательства: в 7 сессиях поручение закончилось, а сообщения нет 35–50
  минут, пока не спросишь «ну что там?». Вживую 24.09 Госуслуги ждали SMS-код,
  и Бро молчал, пока его не спросили. Итоги без содержания: «Корзина
  1 337,10 ₽» без состава, «4 отеля найдены» без списка.
- Где: `agent/schedules/browser-runs.ts` (опрос, `pollLimit`),
  `db/services/browser-runs.ts`, `agent/lib/browser-use/outcome.ts`.
- Сделать: найти, почему завершённые и ждущие ввода запуски не доставляются;
  запрос кода или пароля сразу пишет человеку; метрика «готово, но не
  доставлено дольше 2 минут»; структурный итог (позиции, цены, ссылки, слот,
  условия отмены).
- Готово: запрос кода приходит в чат меньше чем за минуту; итог корзины и
  отелей содержит список.

## P1

### ~~4. Интеграции: Notion и Slack (EN D8: 2 → 7)~~ (сделано в коде)

Сделано: все интеграции перенесены на Composio вместо Vercel Connect, где
коннекторов Notion и Slack так и не появилось. Google — одно подключение
`googlesuper` на почту, календарь, Диск, контакты, Таблицы и Документы;
Notion (`notion-add-task`, `notion-search`, `notion-read`) и Slack
(`slack-send-message`, `slack-search`, `slack-read`) — через управляемый OAuth
Composio; другие приложения (Google Таблицы и Документы, Todoist, Trello,
Linear, GitHub и др.) — через `apps`, где каждая запись идёт через карточку.
Подключение — карточкой входа в чате, `connect_google`/`connect_app` или в
кабинете, отключение удаляет аккаунт в Composio. Тесты —
`tests/agent/tools/connected-apps.test.ts`, `tests/agent/tools/apps.test.ts`.

Осталось владельцу: задать в Vercel переменные `COMPOSIO_*`, после деплоя
заново подключить Google (гранты Vercel Connect не переносятся), Notion и Slack
в `/workspace`. Готово: «Поставь задачу в Notion и напиши Лёше в Slack» после
одобрения реально создаёт задачу и сообщение.

### 5. Покупка до конца (EN D4: 3 → 7, RU 4 и 5)

Ozon, WB, Яндекс ID и Т-Банк стоят на входе; `list_orders` знает только заказы
самого Бро; корзины без состава. Путь: вход через сейф → код от владельца →
корзина с составом → карточка оплаты → оплата → номер заказа. Готово: один
реальный повтор заказа на Ozon с одобрением владельца.

Сделано в коде: поручение «как в прошлый раз» / «повтори заказ» идёт в
историю заказов аккаунта на сайте (`repeatOrderLine`), а не спрашивает, что
именно; позиции корзины (`ITEMS`) попадают в карточку оплаты
(`submission.items`, строка «Состав») и в записанный заказ (`orders.items`,
`list_orders` отдаёт состав); заказ записывается только у запуска, который мог
купить, и «отменить можно до…» больше не помечает его отменённым; итог оплаты
— чек с номером, суммой, составом и доставкой. Осталось: вход на Ozon с кодом
от владельца и один живой повтор заказа.

### ~~6. Проактивность видна везде, отчёты расписаний доходят (EN D6: 4 → 7, D7)~~

Сделано: целью становится последний мессенджер, где человек писал, а веб —
только если мессенджера нет (`agent/hooks/proactive-target.ts`,
`recordProactiveTarget`); отчёт расписания из закончившейся веб-сессии идёт в
чат, где расписание заведено, затем в мессенджер, и только потом
`suppressed`; подключение Google из кабинета, карточки
в чате или `connect_google` будит проверку, отложенную из-за отсутствия гранта
(`wakeProactiveWatch`); отчёты в веб доставляет тик `dynamic` через
`attachSession` вместо своего маршрута, который на Vercel до eve не доходил.
Путь «письмо → сообщение в веб-чат» покрыт `tests/agent/schedules/web-delivery.test.ts`
и `evals/agent/proactive.eval.ts` (без живого Google — с передачи воркера).
~~Осталось: ответ на вопрос расписания (`schedules-answer`) всё ещё идёт через
`/internal/scheduled-run/respond` и в продакшене не доходит.~~ Сделано в
задаче 10: ответ хранится в базе и доходит тиком `dynamic`. Осталось проверить
вживую с настоящим ящиком.

### 7. Групповые чаты (EN D13: 3 → 7, RU 17)

Telegram отбрасывает группы, iMessage принимает только личные номера. Бро в
группе отвечает на упоминание, собирает варианты и голосование, не отвечает на
каждое сообщение и не раскрывает личное владельца.

### ~~8. Точность фактов и честность о себе (EN D3, D5, D15; RU 3, 6, 14)~~

Сделано: инструмент `calculate` (точная арифметика), правила «Точность фактов»
в `execution-safety.md`, раздел «Как ты устроен» в `role/interactive.md`,
эвалы в `evals/agent/honesty.eval.ts`. Нужен перепрогон EN D3, D5, D15.

Неверный расчёт переводов, устаревший лимит вычета 120 000 ₽, «временная
регистрация онлайн не подаётся», «проверил цену» без вызова инструментов,
«данные на твоём собственном сервере», «Notion и Slack подключены» при
`not_configured`. Где: `agent/instructions/content/role/interactive.md`,
`agent/instructions/content/execution-safety.md`, `docs/memory.md`. Сделать:
арифметику пересчитывать инструментом, сроки и лимиты брать из поиска со
ссылкой, о подключениях говорить по реальному статусу.

## P2

9. ~~**Сессия не умирает после деплоя.** FatalError «Dynamic tool callback
   rebind did not restore» посреди разговора (`patches/eve@0.62.0.patch`):
   пересобирать инструменты с нуля, эвал «деплой посреди ожидающей сессии».~~
   Сделано: хунк перепривязки в `patches/eve@0.62.0.patch`, деплой посреди
   сессии симулирует `tests/agent/dynamic-tool-rebind.test.ts` (эвал eve
   процесс не перезапускает).
10. ~~**Расписания помесячно и из любого чата.** «Каждое 5-е» стало интервалом
    43 200 минут; `schedules-list` в новом чате пуст. `shared/schedules/timing.ts`,
    `agent/lib/schedules/tools.ts`.~~ Сделано: календарные правила по числу
    месяца (с «последним днём» и переносом 29–31), n-му дню недели, дням
    недели и раз в год в таймзоне человека; расписания видны и правятся из
    любого чата, отчёт идёт в последний чат человека; ответ на вопрос
    расписания доходит через тик `dynamic`. Нужен перепрогон RU 12, EN D7;
    старые расписания «раз в 43 200 минут» так и остались интервалами.
11. ~~**Русский язык.** Бро о себе в женском роде («сделаю сама»), опечатки,
    сохранённое «на вы» не держится в других чатах.
    `agent/instructions/content/message-style.md`, `agent/lib/delivery/language.ts`.~~
    Сделано: мужской род и обращение в пометке каждого шага, «вы» и имя в
    `settings.form_of_address` (инструмент `form_of_address`), частые ошибки
    русской речи в `message-style.md`, эвалы `evals/agent/language.eval.ts`.
12. ~~**Модель по замеру.**~~ Сделано: после прогона на `openai/gpt-6-luna`
    владелец (24.09) вернул модель по умолчанию на
    `deepseek/deepseek-v4.1-flash`, A/B не нужен; картинки —
    `google/gemini-3.1-flash-lite-image`. Кабинеты с явно выбранной моделью
    остаются на своей. Вживую проверены `tool_choice: required` у провайдеров
    DeepSeek на OpenRouter, конец хода после доставки и картинка с правкой;
    найдено и исправлено: часть хостов выбирала ветку union в вызове
    инструмента по порядку ключей схемы (расписания «каждое 5-е» и разовые,
    `replyTo` отчётов) — `docs/dev-notes.md`, раздел OpenRouter. Нужен
    перепрогон RU.
13. ~~**Бенчмарк в репозитории.** Закоммитить драйвер (по `docs/benchmarks/README.md`,
    с разбором событий через zod и окружением через `shared/environment`),
    научить слать фото и голос; завести тестовый ящик с заготовками писем.~~
    Сделано: `scripts/bench/`, `pnpm bench`; заготовки —
    `pnpm bench fixtures seed|clean|list`: письма, события и файл в Диске в
    Google тестировщика через Composio (RU d09–d12, d14, d18 и сценарии с
    чеками; EN D5–D8, D14, D15). Прогон с ними — задача 23.
14. ~~**Лавина сообщений, «сделал» без действия, пустые проактивные отчёты**
    (RU 24.09: d02, d05, d06, d15, d18).~~ Сделано: после ответа в ходе
    проходит только сообщение с новым, лимит 3, после отброса `toolChoice:
none`; пометка шага после доставки не читается как новая просьба;
    «ввёл код», «ставлю в календарь» без действия возвращаются на
    переписывание; пустая передача воркера не запускает ход-отчёт; правило
    «код — сразу `NEEDS`» стоит первым в тексте поручения. Нужен перепрогон
    RU d02, d05, d06, d15, d18.
15. ~~**gpt-6-luna: проваленные ходы, лишние вопросы и картинки** (RU 24.09
    luna2: пустой ответ после доставки в 10 из 12 диалогов, «Сохранить их?»
    в d13, «Какие действия выполнить?» в d15, робот без просьбы в d06).~~
    Сделано: пустой шаг после доставки завершает ход, `ask_question` с
    описанием «не подтверждай прямую просьбу» и не больше одного за ход,
    `generate_image` только в ходе с просьбой о картинке или фото.
    ~~Статус «пока ищу» и ещё одно сообщение в том же ходе~~ — сделано: первое
    сообщение хода, которое только объявляет ещё не начатую работу,
    возвращается на переписывание, статус остаётся делу в фоне (запущенному
    поручению), а реакция без слов закрывает ход, только если человек ни о
    чём не спрашивал; поправка итога — в задаче 20. ~~`continue` завершённого запуска на «ну что
    там?» вместо пересказа итога (d01, d02), расписание, которого не просили
    (d02)~~ — см. задачи 18 и 21.
16. ~~**Итоги браузерных поручений не приходят сами** (RU 24.09 luna2: d01,
    d02, d04, d05, d07 — итог только после «ну что там?»; `continue` с
    карточкой до поиска в d07; ложное «нет данных для входа» в d06;
    «остановить или оставить?» и удаление без ответа в d02; `send --kind
answer` не досылал сценарий в d13, d15).~~ Сделано: итог доставлен, только
    когда ход-отчёт дошёл до человека (хук), упавший ход-отчёт повторяется,
    простой итог пишется вместе с захватом завершения; тик поллера смотрит
    открытые запуски каждые 4 с (итог за ~5–15 с вместо до минуты); первый
    шаг хода-отчёта обязан вызвать инструмент; `continue` с окном вместо
    варианта отклоняется, пока запуск ищет; результат `browser_task` и
    `request_vault_setup` говорят, что вход сохранён; после вопроса текстом
    удаление и правка расписаний, памяти и календаря ждут ответа; `send
--kind answer` досылает сценарий. Нужен перепрогон RU d01, d02, d04–d07,
    d13, d15.

## Итоги RU-бенча 24.09 на gpt-6-luna

Сборка после #186–#188, модель `openai/gpt-6-luna`, прогон на проде.
Итог ≈ 4.2 против базы 5.2 по тем же 11 измерениям, индекс РФ 3.7 против
4.5. Полностью оценено 9 измерений из 18, поэтому итог не публикуется.

| Изм. | База | luna | Главная причина                       |
| ---- | ---- | ---- | ------------------------------------- |
| d01  | 4    | 2    | итог браузера не пришёл               |
| d02  | 4    | 3    | итог только после подсказки           |
| d03  | 6    | 3    | два непроверенных варианта            |
| d04  | 3    | 2    | нет пароля Ozon в сейфе               |
| d05  | 5    | 5    | адрес не попал в поручение            |
| d06  | 6    | 6    | —                                     |
| d07  | 3    | 2    | карточка до поиска, нет пароля mos.ru |
| d08  | —    | 3    | ошибка в первом сообщении             |
| d14  | 6    | 6    | —                                     |
| d15  | 6    | 5    | —                                     |

Лишние вопросы и падения хода после ответа сняты в #189, доставка итогов
браузера — в задаче 16. Оба исправления вышли после прогона.

17. ~~**P1. Качество подбора (d03).**
    - Было: два `web_search` из трёх упали по таймауту; Карты и 2ГИС не
      открывались; цены, столы и «не сеть» не проверены.
    - Сделать: надёжный поиск с повтором, проверка ограничений по источникам,
      а при нехватке данных — честно меньше вариантов.~~
    - Сделано: `web_search` ищет через Exa, а при таймауте, 429/5xx или пустой
      выдаче — через Perplexity (было — родной поиск OpenAI: 5–26 с и без
      ссылок); выдача несёт отрывок страницы с чеком, часами и адресом;
      `sites` ищет по картам и агрегаторам. `web_fetch` ждёт 12 с, на капче,
      403/429 и таймауте отвечает, чем заменить, и не ходит на этот сайт
      несколько минут. Раздел «Подбор под условия»
      (`agent/instructions/content/recommendations.md`), эвалы
      `evals/agent/recommendations.eval.ts`. Нужен перепрогон RU d03 и EN
      `d03_reco`.
18. ~~**P1. Доводить дело до конца.**~~
    - Было:
      - d02: не выяснено окно регистрации на рейс, проверка регистрации не
        заведена;
      - d08: квитанцию не искал, показания не переданы;
      - d18: цепочка встала после первого шага.
    - ~~Сделать: следующий шаг ставить сразу — расписание или напоминание —
      вместо «напиши, если нужно».~~
    - Сделано: раздел «Дело до конца» в `autonomy.md` — просьба из нескольких
      частей делается целиком, шаг на потом сразу ставится `schedules-create`
      с названным временем, но только для дела, которое человек поручил
      (слежку и напоминания, о которых не просил, — только предложить);
      запуск отдаёт момент шага на потом строкой `NEXT:`, ход-отчёт по ней
      ставит расписание или только называет срок. Эвалы —
      `evals/agent/follow-through.eval.ts` (не прогонялись). Нужен перепрогон
      RU d02, d08, d18.
19. ~~**P1. Данные профиля в первое поручение.** В d05 адрес доставки не попал
    в первый запуск, понадобился второй. Известные факты (`known facts`)
    должны идти в текст поручения сразу.~~ Сделано: поручение о доставке
    (`deliveryAddress` или слова доставки в тексте) и без карточки получает
    сохранённый адрес доставки — без имени, телефона и почты — только для
    выбора адреса на сайте; остальное по-прежнему ждёт карточки. Нужен
    перепрогон RU d05.
20. ~~**P1. Одно сообщение на итог.**~~
    - Было: дубли итогов (d05, d06, d08, d18); в d08 Бро сам себя поправлял
      («вода», потом «электросчётчик»).
    - Сделано: ход-отчёт браузера после первого сообщения пропускает только
      новый вопрос, просьбу (код, подтверждение, выбор), картинку или ссылку;
      пересказ, дополнение и поправка того же итога отбрасываются
      (`reported`), а `continue` с карточкой после отброса ещё проходит.
      Ход-отчёт, чей итог человек уже услышал из `browser_task status`,
      кончается молча. Тесты — `agent/lib/delivery/tests/turn-sends.test.ts`,
      `tests/agent/agent.test.ts`. Нужен перепрогон RU d05, d06, d08, d18.
21. ~~**P1. Особенности luna.**~~
    - Было: ~~на «ну что там?» — `continue` завершённого запуска вместо
      пересказа итога (d01, d02); расписание, которого не просили (d02);~~
      ~~ответ на `ask_question` ушёл в `schedules-answer` (d15).~~
    - Сделано (поручения): `continue` завершённого запуска, чей итог человек
      ещё не слышал, в ходе человека отвечает этим итогом, а не новым
      запуском; `status` у завершённого запуска отдаёт итог с указанием
      пересказать его; расписание — только для шага дела, о котором просили
      (задача 18). Нужен перепрогон RU d01, d02.
    - Сделано (доставка): `schedules-answer` есть только в ходе, который
      отвечает на вопрос расписания, а в ходах-отчётах нет `ask_question`, так
      что ответ на вопрос отчёта начинает ход человека.
    - ~~A/B-замер моделей~~ не нужен: владелец выбрал
      `deepseek/deepseek-v4.1-flash` (задача 12).
22. ~~**P2. Права и доступы.**~~
    - ~~d14: правило «без моего ок» не записывается в память; на фразу о
      правилах — две лишние карточки (`standing_permission revoke`,
      `spend_limit clear`); непрошеный вопрос «удалить данные?».~~ Сделано:
      правило — запись профиля с `category: "rule"`, память показывает
      правила первым разделом как ограничения, а лимит трат и разрешения,
      которые правило запрещает, снимаются в том же ходе; снятие и сужение
      идут без карточки. Причина карточек — luna шлёт `merchant: ""` вместо
      пропуска, а политика читала это как расширение: теперь пустой или `*`
      магазин и сайт значат «любой», а прочее неприменимое отклоняется с
      причиной, а не карточкой; без лимита и разрешений инструкции и ответы
      инструментов говорят «снимать нечего»; «удалить?» без просьбы не
      спрашивается. Эвалы — `evals/agent/permissions.eval.ts`. Нужен
      перепрогон RU d14.
    - ~~d06: после отказа от Google только на чтение сразу просит полный доступ.~~
      Сделано в задаче 4: отказ записи в режиме только чтения говорит, что
      действие недоступно, и полный доступ не предлагает.
23. **P2. Покрытие бенчмарка до публикуемого итога (≥ 12 измерений).**
    - ~~Драйвер для почты, наблюдения и многодневных кейсов~~ (сделано):
      заготовки (`fixtures seed`), наблюдение за тем, что Бро пишет сам
      (`observe`, `send --kind observed`), шаги «T+…» в срок (`run`, `next`),
      `probe` и `cleanup` без счёта подсказок; добавлены EN D5, D6, D14, D15.
      Порядок — `docs/benchmarks/README.md`. Вживую 25.09: `run` и `next` d13
      (T+0, затем `scheduled`), `observe` своего разговора и ветки Telegram,
      поиск подключения Composio по cookie. Засев в Google проверен только
      тестами: подключения `googlesuper` у тестировщика ещё нет.
    - От владельца: подключить Google тестировщика в Composio (`googlesuper`,
      после переезда интеграций), пароли Ozon и mos.ru в сейфе.
    - Нужно прогнать d09–d12, d14, d18 с заготовками, d13 и d15 — по дням.
      d16 и d17 — N/A: звонков нет, группы владелец исключил.

## Что нужно от владельца

- Переменные `COMPOSIO_*` в Vercel и подключение Google, Notion и Slack в
  кабинете после деплоя (задача 4).
- Кредиты Browser Use (24.09 пополнено на $10) и OpenRouter для картинок.
- Коды входа вживую для Ozon, mos.ru (ЕМИАС), Яндекс ID, WB, Т-Банка; логины
  Ozon и mos.ru в сейфе Бро; Google тестировщика, подключённый в Composio, —
  в него `pnpm bench fixtures seed` кладёт письма (задача 23).
